<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projet">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Detail_event" />
        <activity
            android:name=".LesEvenements"
            android:label="@string/title_activity_les_evenements"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_modif_compte">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".Modif_compte" />
        <activity
            android:name=".InscriptionActivity"
            android:label="@string/title_activity_inscription" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".CreateEventActivity" />
        <activity android:name=".ModifEventActivity" />
        <activity android:name=".ListEventAdminActivity" />
        <activity android:name=".detail_event_admin" />
        <activity android:name=".ListPseudoAdminActivity" />
    </application>

</manifest>